const search=document.querySelector('#search');const buttons=[...document.querySelectorAll('[data-filter]')];const cards=[...document.querySelectorAll('.day-card')];const empty=document.querySelector('#empty');let active='all';function apply(){const q=search.value.trim().toLowerCase();let shown=0;cards.forEach(card=>{const weekOk=active==='all'||card.dataset.weekday===active;const searchOk=!q||card.dataset.search.includes(q);const visible=weekOk&&searchOk;card.hidden=!visible;if(visible)shown++;});empty.hidden=shown!==0;}buttons.forEach(btn=>btn.addEventListener('click',()=>{active=btn.dataset.filter;buttons.forEach(b=>b.classList.toggle('active',b===btn));apply();}));search.addEventListener('input',apply);